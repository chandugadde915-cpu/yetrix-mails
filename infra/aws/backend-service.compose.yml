services:
  backend:
    image: YOUR_AWS_ACCOUNT_ID.dkr.ecr.YOUR_REGION.amazonaws.com/ownmail-backend:latest
    environment:
      NODE_ENV: production
      PORT: 4000
      CORS_ORIGIN: https://your-vercel-frontend-domain
      MAILCOW_BASE_URL: https://mail.yetrixmails.com
      MAILCOW_API_KEY: SET_FROM_SECRET_MANAGER
      MAIL_DOMAIN: yetrixtechnologies.com
      MAIL_SERVER_IP: 56.228.11.175
    ports:
      - "4000:4000"
